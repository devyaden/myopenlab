// Phase 2: stable ids & the cross-reference spine. Codes (human-readable,
// unique-per-user identifiers) + the reference resolver (code → entity,
// backlinks, integrity checks).
export * from "./codes";
export * from "./resolver";
